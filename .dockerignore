# Never bake live data into the image — must come from the mounted volume
data/state.json
data/parses.json

# Don't include dev artifacts in the image
.env
.env.*
!.env.example
*.log
node_modules
.git
.gitignore
README.md
